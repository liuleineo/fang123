# 2026-08-18 一房一价增加预售证号/房屋编码字段

## 需求

`loupan_yfyj` 表新增 `permit_no`（预售许可证编号）和 `fwcode`（房屋编码）两个字段（schema.sql 第 512-513 行，位于 huxing_id 之后），admin 一房一价管理页（/admin/yfyj）在"户型id"后面显示这两个字段。

## 改动内容

### 1. schema.sql
`loupan_yfyj` 表在 `huxing_id` 之后新增：
- `permit_no` varchar(100) 预售许可证编号
- `fwcode` varchar(100) 房屋编码

### 2. 实体 LoupanYfyj.java
新增字段 `permitNo` / `fwcode`（位于 `huxingId` 之后）

### 3. admin LoupanYfyjManage.vue
- 表格列：在"户型ID"列后新增"预售证号"、"房屋编码"两列
- 表单：在"户型ID"输入框后新增"预售证号"、"房屋编码"输入框
- `initForm()` 添加 `permitNo:''`、`fwcode:''`

### 4. 本机数据库
执行 ALTER TABLE 为本机 `fang123.loupan_yfyj` 添加 `permit_no`、`fwcode` 字段（已验证字段存在）

## 编译

- admin 编译成功（LoupanYfyjManage-CJeT_TJQ.js）
- 后端 jar 编译成功（`fang123-backend-1.0.1-SNAPSHOT.jar`）

## 待办

- 线上数据库需执行同样的 ALTER TABLE（`alter_loupan_yfyj_20260818.sql`）
- 需部署 admin dist + 后端 jar 并重启后端进程后线上生效
