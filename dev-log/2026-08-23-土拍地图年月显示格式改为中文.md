# 2026-08-23 土拍地图年月显示格式改为"xxxx年xx月"

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

- `ym()` 辅助函数返回格式从 `2025-03` 改为 `2025年03月`（无月份时 `2025年`）
- 列表项年份标签、地图 label 括号内年份、筛选下拉 label/value 均随 `ym()` 自动更新为中文年月格式

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
