# 2026-08-13 map 点击楼盘改为打开新窗口详情页

## 改动内容

`web/src/views/MapSearch.vue`（/map 地图找房页面）：

- 移除点击标记时的 InfoWindow 弹窗逻辑（含 `typeStr`、`infoWindow` 相关代码）
- 点击标记改为 `window.open('/loupan/${lp.encodedId}', '_blank')` 新窗口打开楼盘详情页
- 保留地图标记上的名称+价格 label 展示

## 交互效果

地图上点击楼盘标记 → 直接在新标签页打开该楼盘详情页，不再弹出 InfoWindow 小窗。

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 若需线上生效，需将 `web/dist` 部署到线上服务器
