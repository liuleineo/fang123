# 2026-09-08 楼盘动态"新建动态/编辑"由抽屉改为弹窗

## 需求

管理端楼盘动态页（`/admin/dynamics`）"新建动态"（及"编辑动态"）表单由右侧抽屉 `t-drawer` 改为居中弹窗 `t-dialog`。

## 改动

`admin/src/views/LoupanDynamicManage.vue`：

- `<t-drawer ... size="560px">` → `<t-dialog ... width="640px" :close-on-overlay-click="false">`
- 闭合标签 `</t-drawer>` → `</t-dialog>`
- 表单结构、图片上传/粘贴、保存按钮不变；宽度 640px 容纳图片管理区

## 备注

- 纯前端；部署重新上传 `admin/dist`
