# 2026-08-25 预售证表：删"公示结束日期"，增"公示日期、核发日期"

## 需求

预售证表 `loupan_presale_permit` 删掉"公示结束日期"(publicity_end_date)，增加"公示日期"(publicity_date)、"核发日期"(issue_date)。

## 改动内容

### 数据库
- `schema.sql`：`publicity_end_date` → 拆为 `publicity_date`（公示日期）+ `issue_date`（核发日期）
- 本机 `loupan_presale_permit` 表 ALTER：DROP `publicity_end_date`，ADD `publicity_date`、`issue_date`（均 DATE，置于 project_name 后）

### 后端
1. `entity/LoupanPresalePermit.java`：`publicityEndDate` → `publicityDate` + `issueDate`
2. `controller/LoupanPublicController.java`：公开预售证列表按 `issueDate` 倒序
3. `dto/AiParsePresaleResult.java`：`publicityEndDate` → `publicityDate` + `issueDate`
4. `service/AiParseService.java`：预售证 AI 解析提示词字段改为 `publicityDate`/`issueDate`

### admin 前端
- `PresalePermitManage.vue`：
  - 表单"公示结束日期"改为"公示日期"+"核发日期"两个日期选择器
  - initForm 同步更新
  - 表格列"公示结束日期"改为"公示日期"、"核发日期"两列

### web 前端
- `LoupanDetail.vue` 开盘信息表：表头/单元格由"公示结束日期"改为"公示日期"+"核发日期"两列

## 编译

- 后端 jar 编译成功并已重启（预售证接口 401 正常）
- admin 编译成功
- web 编译成功

## 待办

- 线上需 ALTER `loupan_presale_permit` 表 + 部署后端 jar + admin dist + web dist
