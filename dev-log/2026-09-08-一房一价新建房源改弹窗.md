# 2026-09-08 一房一价"新建房源/编辑"由抽屉改为弹窗

## 需求

管理端一房一价页（`/admin/yfyj`）"新建房源"（及"编辑房源"）表单由右侧抽屉 `t-drawer` 改为居中弹窗 `t-dialog`。

## 改动

`admin/src/views/LoupanYfyjManage.vue`：

- `<t-drawer ... size="450px">` → `<t-dialog ... width="560px" :close-on-overlay-click="false">`
- 闭合标签 `</t-drawer>` → `</t-dialog>`
- 表单结构、字段、"保存"按钮不变；宽度 560px 适配两列表单
- 页面内原有"批量设置"、AI 新建弹窗不受影响

## 备注

- 纯前端；部署重新上传 `admin/dist`
