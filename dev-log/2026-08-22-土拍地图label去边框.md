# 2026-08-22 土拍地图去掉地块 label 边框

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

1. 地块 label 内容 div 内联样式新增 `border:none;outline:none`
2. 全局 `<style>` 块新增覆盖高德 label 外层容器边框：
   ```css
   .amap-marker-label {
     border: none !important;
     background: transparent !important;
   }
   ```
   （高德动态生成的 label 容器不在组件 scoped 作用域内，需全局样式覆盖其默认边框）

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
