# 2026-09-08 媒体素材"关联户型"按所选楼盘联动选择

## 需求

管理端媒体素材页（`/admin/medias`）"新建素材/新建视频素材/编辑"：选择楼盘 ID 后，"关联户型"字段可选且只列出该楼盘的户型（不再手填数字）。

## 改动

### 后端 `backend/.../controller/LoupanHuxingController.java`
- 新增 `GET /api/admin/huxings/options?loupanId=`：返回该楼盘户型列表 `[{id,huxingName,area}]`（loupanId 为空返回空数组；逻辑删除自动过滤）

### 前端 `admin/src/views/LoupanMediaManage.vue`
- 表单"关联户型(可选)"由 `t-input-number` 改为 `t-select`（可搜索/可清空）
  - 未选楼盘时禁用，提示"先选择楼盘，再选择该楼盘户型"
  - 监听 `form.loupanId`：切换楼盘时清空已选户型并重新拉取户型选项
- 选项文案：`id · 户型名（面积㎡）`；`openEdit` 打开时按 row.loupanId 加载户型

## 备注

- 两端改动；已重新构建 `admin/dist` 与后端 jar
- 本地后端已重启（PID 2753）供 dev 联调；生产需部署新 admin/dist + 新 jar
