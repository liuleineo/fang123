# 2026-08-22 土拍地图右下角显示缩放级别

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

1. 新增 `currentZoom` ref（默认 13）
2. `syncLabelVisibility()` 内更新 `currentZoom`（随地图缩放变化）
3. 地图容器右下角（卫星按钮上方）新增缩放级别显示：`级别 {{ currentZoom }}`（白底半透明标签）

## 交互效果

地图右下角实时显示当前缩放级别（如"级别 13"），随缩放动态更新。

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
