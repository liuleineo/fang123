# 2026-08-13 map 地图只展示在售/待售楼盘

## 改动内容

`web/src/views/MapSearch.vue`（/map 地图找房页面）：

- `fetchData()` 请求 `/public/loupans` 时新增 `salesStatus: '0,1'` 参数，只返回**待售（0）**和**在售（1）**状态的楼盘，排除售罄（2）和已交付（3）

## 后端说明

后端 `/api/public/loupans` 接口已支持 `salesStatus` 逗号分隔多值筛选（`w.in(Loupan::getSalesStatus, statusList)`），无需改动后端。

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 若需线上生效，需将 `web/dist` 部署到线上服务器
