# 2026-08-18 预售证管理表格增加预售许可证编号列

## 改动内容

`admin/src/views/PresalePermitManage.vue`（/admin/presale-permits 预售证管理）：

1. `permitNoStr` 列标题从"预售证编号"改为"预售证编号STR"
2. 在其后新增 `permitNo` 列，标题"预售许可证编号"，展示 `row.permitNo`（如 202600455）

## 说明

- 已重新编译 admin 端（`npm run build`）成功，产物 `PresalePermitManage-1NNJKivQ.js`
- 若需线上生效，需将 `admin/dist` 部署到线上服务器
