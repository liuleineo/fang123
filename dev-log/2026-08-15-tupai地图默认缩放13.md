# 2026-08-15 土拍地图默认缩放级别改为13

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

- 地图初始化 `new AMap.Map('amap-container', {...})` 的 `zoom` 从 12 改为 13
- 点击列表项的 `setZoomAndCenter(16)` 不受影响（保持交互缩放）

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 若需线上生效，需将 `web/dist` 部署到线上服务器
