# 2026-08-22 土拍地图地块 label 改回一行显示

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

- 地块 label 由两行（地块名 / 年份）改回一行显示：`地块名（年份年）`
- 保留去边框样式（border:none;outline:none）

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
