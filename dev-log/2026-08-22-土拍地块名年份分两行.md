# 2026-08-22 土拍地图地块名和年份分两行显示

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

- 地块 label 内容由一行（地块名（年份年））改为两行：
  - 第一行：地块名（加粗，最大宽度 140px，超长省略）
  - 第二行：年份（小字，半透明，如"2025年"）
- label 整体 padding 调整为 3px 8px，圆角 6px，文字居中

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
