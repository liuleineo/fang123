# 首页搜索区紧凑化

## 修改内容
`web/src/views/Home.vue`

1. **Hero 区域**：上下 padding 从 `py-16 lg:py-24` 缩小为 `py-10 lg:py-14`
2. **标题**：字号从 `text-4xl/5xl/6xl` 缩小为 `text-3xl/4xl/5xl`，mb 从 6 缩小为 4
3. **副标题**：字号从 `text-base/sm:text-lg` 缩小为 `text-sm/sm:text-base`，mb 从 10 缩小为 6
4. **筛选栏**：上下 padding 从 `py-6` 缩小为 `py-3`，间距从 `gap-3` 缩小为 `gap-2`
5. **热门标签**：mt 从 5 缩小为 4
