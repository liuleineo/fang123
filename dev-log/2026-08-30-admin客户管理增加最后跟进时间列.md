# 2026-08-30 admin 客户管理增加"最后跟进时间"列

## 需求

`http://localhost:3002/admin/customers` 客户管理列表，在"创建时间"列前增加"最后跟进时间"列。

## 改动

### 后端（CustomerController.java）

- 注入 `FollowUpService`
- 新增 `fillLastFollowUp` 方法：按客户 ID 查询 `follow_ups`（按跟进时间倒序），取每客户最新一条，填充 `lastFollowUpTime` / `lastFollowUpContent`
- 列表接口与详情接口均调用

### 前端（admin/src/views/CustomerManage.vue）

- 表格 `cols` 中"备注"后、"创建时间"前新增 `lastFollowUpTime` 列（宽度 160）
- 新增 `#lastFollowUpTime` 插槽：有值展示格式化时间，无值显示 "-"

## 验证

- 后端编译重启成功，接口实测：客户"王女士"正确返回最后跟进时间 2026-08-30 21:59:51 及内容"看了沐青云"，无跟进记录的客户返回空
- admin 编译通过，dev 已热更新
