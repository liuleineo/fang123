# 2026-08-13 map 移除地图标记 label 边框

## 改动内容

`web/src/views/MapSearch.vue`（/map 地图找房页面）：

1. 标记 label 内容 div 内联样式新增 `border:none;outline:none`
2. 新增全局 `<style>` 块覆盖高德地图 label 外层容器：
   ```css
   .amap-marker-label {
     border: none !important;
     background: transparent !important;
   }
   ```
   （因高德动态生成的 label 容器不在组件 scoped 作用域内，需用全局样式覆盖其默认边框）

## 交互效果

地图上楼盘标记的名称+价格 label 不再显示边框，视觉更干净。

## 说明

- 已重新编译 web 端（`npm run build`）成功，新增 `MapSearch-qn-o5X3p.css` 样式文件
- 若需线上生效，需将 `web/dist` 部署到线上服务器
