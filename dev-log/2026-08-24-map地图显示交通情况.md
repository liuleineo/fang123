# 2026-08-24 /map 地图显示当前交通情况

## 改动内容

`web/src/views/MapSearch.vue`（/map 地图找房页面）：

- 地图初始化时创建交通路况图层 `AMap.TileLayer.Traffic()` 并 `mapInstance.add(trafficLayer)`，默认显示当前实时交通情况（道路拥堵红/黄/绿色标识）

## 交互效果

进入 /map 地图即显示当前路况，实时反映道路拥堵情况。

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
