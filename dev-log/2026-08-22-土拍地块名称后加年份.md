# 2026-08-22 土拍地图地块名称后加年份

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

1. **侧边栏列表项**：地块名称 `landName` 后新增年份标签（从 `dealDate` 取前4位，蓝色 badge，如"2025年"），年份从原来的 `landNo` 旁移到名称后
2. **地图标记 label**：地块名称后新增年份（如"XXX（2025年）"）

## 交互效果

- 侧边栏地块名称后显示年份
- 地图标记上的地块名称后也显示年份（括号形式）

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 若需线上生效，需将 `web/dist` 部署到线上服务器
