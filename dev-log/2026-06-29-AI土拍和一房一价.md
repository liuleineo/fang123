# 2026-06-29 AI 土拍地块 + 一房一价

## 土拍地块
- 新增 `AiParseTupaiLandResult` DTO
- `POST /api/admin/tupai-lands/ai-parse` 接口
- `LoupanTupaiLandManage.vue` 新增"AI新建地块"按钮 + 对话框

## 一房一价
- 新增 `AiParseYfyjResult` DTO（支持批量）
- `POST /api/admin/yfyj/ai-parse` 接口
- `LoupanYfyjManage.vue` 新增"AI新建房源"按钮 + 对话框（勾选批量创建）

## 通用优化
- `AiParseService` 提取 `doOcrBatch()` + `callTokenHub()` 通用方法
- 新增土拍、一房一价专用提示词
