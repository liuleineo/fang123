# 2026-08-20 一房一价分页增加每页100条/200条选项

## 改动内容

`admin/src/views/LoupanYfyjManage.vue`（/admin/yfyj 一房一价）：

- `pg` 分页配置新增 `pageSizeOptions: [10, 20, 50, 100, 200]`，分页器每页条数选项增加 100 条、200 条

## 说明

- 已重新编译 admin 端（`npm run build`）成功
- admin dev 服务器（3002）已热更新
- 若需线上生效，需将 `admin/dist` 部署到线上服务器
