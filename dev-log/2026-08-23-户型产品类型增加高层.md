# 2026-08-23 户型产品类型(floorType)增加"高层"

## 改动内容

户型产品类型下拉/映射增加"高层"（value=0），置于"小高层"之前。

### admin LoupanHuxingManage.vue
- `floorType` 下拉选项新增 `{label:'高层',value:0}`，排在"小高层"前

### web LoupanSubPage.vue
- floorType 映射数组由 `['','小高层','洋房','叠墅','排屋']` 改为 `['','高层','小高层','洋房','叠墅','排屋']`（index0=高层）

### 后端
- schema.sql：`floor_type` 注释改为 `0高层 1小高层 2洋房 3叠墅 4排屋`
- AiParseService.java：AI 提示词 floorType 增加 `0=高层`

### 本机数据库
- `floor_type` 默认值从 1 改为 0（`ALTER TABLE MODIFY`，已验证）

## 说明

- value 值分配：0=高层，1=小高层，2=洋房，3=叠墅，4=排屋（不改变已有 1-4 语义，新增 0 为高层）
- 已编译 admin / web / 后端 jar 全部成功

## 待办

- 线上数据库需同步 `floor_type` 默认值为 0（可选，仅影响新建默认）
- 需部署 admin dist + web dist + 后端 jar 并重启后端
