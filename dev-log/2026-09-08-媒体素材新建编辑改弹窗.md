# 2026-09-08 媒体素材"新建素材/新建视频素材/编辑"由抽屉改为弹窗

## 需求

管理端媒体素材页（`/admin/medias`）"新建素材"、"新建视频素材"（及"编辑素材"，共用同一表单）由右侧抽屉 `t-drawer` 改为居中弹窗 `t-dialog`。

## 改动

`admin/src/views/LoupanMediaManage.vue`：

- `<t-drawer ... size="450px">` → `<t-dialog ... width="640px" :close-on-overlay-click="false">`
- 闭合标签 `</t-drawer>` → `</t-dialog>`
- 表单含"素材URL（上传文件/粘贴图片）"上传区与视频上传进度逻辑，640px 宽度容纳；其余字段与保存逻辑不变
- 页面内"批量新建素材"弹窗不受影响

## 备注

- 纯前端；部署重新上传 `admin/dist`
