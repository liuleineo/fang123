# 2026-09-08 预售证"新建预售证/编辑"由抽屉改为弹窗

## 需求

管理端预售证页（`/admin/presale-permits`）"新建预售证"（及"编辑预售证"）表单由右侧抽屉 `t-drawer` 改为居中弹窗 `t-dialog`。

## 改动

`admin/src/views/PresalePermitManage.vue`：

- `<t-drawer ... size="500px">` → `<t-dialog ... width="600px" :close-on-overlay-click="false">`
- 闭合标签 `</t-drawer>` → `</t-dialog>`
- 字段与保存逻辑不变；AI 新建预售证弹窗不受影响

## 备注

- 纯前端；部署重新上传 `admin/dist`
