# 2026-08-23 土拍地图筛选按年份、显示按年月

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

- **成交时间筛选下拉**：改回只按年份（label `2025年`，value `2025`），筛选用 `substring(0,4)` 匹配年份
- **显示**（列表项年份标签、地图 label 括号内）：保持 `ym()` 的 `2025年03月` 年月格式

即：显示用年月，筛选用年份。

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
