# 2026-09-08 楼盘户型"新建户型/编辑"由抽屉改为弹窗

## 需求

管理端户型管理页（`/admin/huxings`）"新建户型"（及"编辑户型"）表单由右侧抽屉 `t-drawer` 改为居中弹窗 `t-dialog`。

## 改动

`admin/src/views/LoupanHuxingManage.vue`：

- `<t-drawer ... size="450px">` → `<t-dialog ... width="720px" :close-on-overlay-click="false">`
- 闭合标签 `</t-drawer>` → `</t-dialog>`
- 表单含"户型图 / 标准户型图"双上传区、多列网格，宽度 720px 以容纳；其余字段与保存逻辑不变
- 页面内 AI 新建户型弹窗不受影响

## 备注

- 纯前端；部署重新上传 `admin/dist`
