# 2026-08-13 loupan 详情页去掉拨打电话按钮

## 改动内容

`web/src/views/LoupanDetail.vue`（楼盘详情页）：

1. 移除头部信息区的"拨打电话"操作按钮（`<div v-if="loupan.salesTel">` 整块）
2. 移除 `Phone` 图标导入
3. 移除 `copyTel()` 函数及不再使用的 `MessagePlugin` 导入

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 若需线上生效，需将 `web/dist` 部署到线上服务器
