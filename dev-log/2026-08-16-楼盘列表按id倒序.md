# 2026-08-16 楼盘列表按 id 从高到低排序

## 改动内容

`backend/src/main/java/com/fang123/controller/LoupanController.java` 的 `GET /api/admin/loupans` 列表接口：

- 排序从 `orderByDesc(sort).orderByDesc(createTime)` 改为 `orderByDesc(id)`（按 id 从高到低）

## 说明

- 已重新编译后端 jar（`fang123-backend-1.0.1-SNAPSHOT.jar`）
- 需部署 jar 并重启后端进程后线上生效
