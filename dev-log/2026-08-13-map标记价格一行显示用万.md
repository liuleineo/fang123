# 2026-08-13 map 地图标记名称和价格一行显示，价格改为万

## 改动内容

`web/src/views/MapSearch.vue`（/map 地图找房页面）：

1. 楼盘标记 label 从两行（名称 / 价格）改为一行水平展示（flex 布局，名称 + 价格并排）
2. 价格格式改为"万"：优先显示总价 `minTotalPrice-maxTotalPrice万`，无总价时降级显示均价 `avgUnitPrice元/㎡`，再降级"价格待定"

## 交互效果

地图上每个楼盘标记一行内同时展示楼盘名称和价格（总价单位万）。

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 若需线上生效，需将 `web/dist` 部署到线上服务器
