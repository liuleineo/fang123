# 2026-08-22 土拍地图缩放级别显示改为仅大于14时显示

## 改动内容

`web/src/views/TupaiMap.vue`（/tupai-map 土拍地图页面）：

- 缩放级别显示增加 `v-if="currentZoom > 14"`，仅当缩放级别大于 14 时在右下角显示"级别 xx"，小于等于 14 时不显示

## 说明

- 已重新编译 web 端（`npm run build`）成功
- 本地 dev（4000）已热更新
- 若需线上生效，需将 `web/dist` 部署到线上服务器
