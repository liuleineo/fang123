# 2026-09-08 真实成交"新建成交/编辑"由抽屉改为弹窗

## 需求

管理端真实成交页（`/admin/real-deals`）"新建成交"（及"编辑成交记录"）表单由右侧抽屉 `t-drawer` 改为居中弹窗 `t-dialog`。

## 改动

`admin/src/views/RealDealManage.vue`：

- `<t-drawer ... size="560px">` → `<t-dialog ... width="560px" :close-on-overlay-click="false">`
- 闭合标签 `</t-drawer>` → `</t-dialog>`
- 表单结构、字段与"保存"按钮保持不变

## 备注

- 纯前端；部署重新上传 `admin/dist`
